{
  "$schema": "https://turborepo.com/schema.json",
  "extends": ["//"],
  "tasks": {
    "test": {
      "dependsOn": ["^build"],
      "inputs": ["workflows/**", "test/**", "vitest.config.ts"],
      "outputs": []
    }
  }
}
